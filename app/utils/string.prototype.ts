// String.prototype.startsWith = function (this: string, term: string): boolean {
//     if (!this)
//         return false;
//     return this.substr(0, term.length) === term;
// };

// String.prototype.contains = function (this: string, term: string): boolean {
//     if (!this)
//         return false;
//     return this.indexOf(term, 0) > 0;
// };
